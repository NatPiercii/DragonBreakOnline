# Full nginx config — installed by Setup-Nginx.bat once certificates exist.
#   api.dragonbreakonline.com       -> backend API     (localhost:4000)
#   dashboard.dragonbreakonline.com -> admin dashboard (localhost:4002)
# Full nginx config, installed by setup_nginx.bat once certificates exist.
#   api.[YOUR WEBSITE HERE]       -> backend API     (localhost:4000)
#   dashboard.[YOUR WEBSITE HERE] -> admin dashboard (localhost:4002)
# Certificates are PEM files exported by win-acme into C:/nginx/certs.
worker_processes 1;

events { worker_connections 1024; }

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    server_names_hash_bucket_size 64;

    ssl_certificate     C:/nginx/certs/api.dragonbreakonline.com-chain.pem;
    ssl_certificate_key C:/nginx/certs/api.dragonbreakonline.com-key.pem;

    # Drop requests for hostnames we don't serve (no response at all).
    server {
        listen 80  default_server;
        listen 443 ssl default_server;
        server_name _;
        return 444;
    }

    # HTTP: keep ACME renewals working, redirect the rest to HTTPS.
    server {
        listen 80;
        server_name api.dragonbreakonline.com dashboard.dragonbreakonline.com;

        location ^~ /.well-known/acme-challenge/ {
            root C:/nginx/html;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # Backend API
    server {
        listen 443 ssl;
        server_name api.dragonbreakonline.com;

        # The client-files zip streams through this proxy.
        client_max_body_size 64m;

        # Launcher installer, served straight from the manager's build output
        # (https://api.dragonbreakonline.com/downloads/DragonBreakLauncher.exe).
        location /downloads/ {
            alias C:/Users/Administrator/Desktop/dragonbreak/build/launcher/;
        }

        location / {
            proxy_pass         http://127.0.0.1:4000;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto https;
            proxy_read_timeout 300s;
        }
    }

    # Admin dashboard
    server {
        listen 443 ssl;
        server_name dashboard.dragonbreakonline.com;

        location / {
            proxy_pass         http://127.0.0.1:4002;
            proxy_set_header   Host              $host;
            proxy_set_header   X-Real-IP         $remote_addr;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto https;
        }
    }
}